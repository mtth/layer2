{
  "name": "dot11",
  "version": "0.1.2",
  "description": "Wi-Fi packets streams!",
  "main": "./lib",
  "scripts": {
    "clean": "rm -rf build node_modules",
    "debug": "node-gyp configure && node-gyp build --debug",
    "test": "$(npm bin)/mocha"
  },
  "engine": "node >= 0.10.33",
  "devDependencies": {
    "mocha": "^2.0.1"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/mtth/dot11.git"
  }
}
