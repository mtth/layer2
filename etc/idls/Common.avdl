protocol Common {

  /**
   * Generic PDU type.
   *
   * This type is used when no matching record was found for the received PDU.
   *
   */
  record Unsupported {
    string name;
  }

  /**
   * MAC address.
   *
   */
  @logicalType("address") fixed MacAddr(6);

}
